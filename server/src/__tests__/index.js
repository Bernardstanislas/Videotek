import { describe, it } from 'mocha';
import { expect } from 'chai';

describe('The whole server', () => {
    it('does not have any test yet', () => {
        expect(true).to.equal(true);
    });
});
